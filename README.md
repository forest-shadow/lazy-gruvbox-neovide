# 💤 LazyVim Neovide Config

Opinionated LazyVim config for Neovide usage on Windows 10.

Neovide config to deal with continuous silent exits after adding LazyVim template: `%APPDATA%\neovide\config.toml`

```toml
frame = "full"
opengl = true
vsync = false
idle = false
srgb = false
fork = false
```
